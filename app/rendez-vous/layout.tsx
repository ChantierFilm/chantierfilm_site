import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Réservez votre Audit de Suivi de Chantier Vidéo & Timelapse',
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
