import { Metadata } from 'next';

export const metadata: Metadata = {
    title: "Portfolio Chantier Film : L'Expertise Vidéo au service de la Construction",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
